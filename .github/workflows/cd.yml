name: cd

on:
    push:
        branches:
            - main

jobs: 
    Deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v3

            - name: setup go
              uses: actions/setup-go@v4
              with:
                  go-version: 1.22.0

            - name: build the app
              run: ./scripts/buildprod.sh

            - name: Authenticate to Google Cloud
              uses: google-github-actions/auth@v3
              with:
                credentials_json: '${{ secrets.GCP_CREDENTIALS }}'
                project_id: 'bootdev-cicd-507910'

            - name: Set up Cloud SDK
              uses: google-github-actions/setup-gcloud@v3

            - name: Build Docker image
              run: |
                gcloud builds submit \
                  --tag us-central1-docker.pkg.dev/bootdev-cicd-507910/notely-ar-repo/notely:latest \
                  .

            - name: Deploy to Cloud Run
              run: gcloud run deploy notely --image us-central1-docker.pkg.dev/bootdev-cicd-507910/notely-ar-repo/notely:latest --region us-central1 --allow-unauthenticated --project bootdev-cicd-507910 --max-instances=4